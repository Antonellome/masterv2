rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Nega tutte le letture e scritture dirette dal client.
    // Solo le Cloud Functions (backend) avranno accesso.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
